# Next experiments

- Run fresh full explain-code-flow subagents on the public Rust and TypeScript workloads; capture wall time and complete parent/child usage.
- Freeze the candidate before a shell/filesystem held-out case and a maintainer-selected second holdout; return only aggregate quality until final selection.
- Add a trusted visual evaluator for text overflow, node overlap, crossings, and PNG presence. Keep it outside candidate scope.
- Compare one-worker-per-figure against two figures per worker only if real token data shows repeated context still dominates; retain parallelism if wall time regresses.
- Consider a machine-readable brief only after real runs show Markdown parsing or duplication is a bottleneck.
