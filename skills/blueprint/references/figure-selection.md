# Figure selection

Select existing atlas figures by the question the plan must answer, following
[the shared consumer procedure](../../system-atlas/references/consume.md).
Record selected figure IDs in `overlay.json`. The catalogue sweep and coverage ledger belong to atlas creation,
not each Blueprint. No separate figure-selection report is required.
