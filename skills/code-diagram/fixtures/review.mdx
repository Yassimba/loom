import { messages } from "./data.ts";

# Submit and process

A caller submits an input. The API normalizes it and hands the accepted request to the queue.

<SequenceDiagram label="Submit request" messages={messages} />
