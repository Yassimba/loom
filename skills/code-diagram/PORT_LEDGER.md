# Review port ledger

Oracle: `devdotfast/review` commit `8267620cd4aee9cc031ae705c4fed6eae0fc9dbe`.

| Review source | Loom destination | Disposition | Reason and parity check |
| --- | --- | --- | --- |
| `skills/dev-review/references/document-authoring.md`, `component-api.md` | `SKILL.md`, fixtures, `examples/loom-installer/` | Public surface preserved | Authors use `review.mdx`, typed `data.ts`, built-in MDX components, and a separate typed `software-map.ts`. No authored `.mjs` remains. |
| `src/authoring.ts:236-548,682-715,896-1153` | `scripts/src/authoring.ts`, `virtual-authoring.ts` | Mechanical port | Keeps strict call-stack/database schemas, `calls()` object identity, target symbols, nested collection handles, `defineStores`, `defineSoftwareActors`, `defineSoftwareStores`, operation direction, and session exports. The document compiler validates every authored map path against the adjacent artifact. |
| `src/compiler/review-document-compiler.ts`, `src/server/doc-bundler.ts:178-270` | `scripts/code-diagram.ts`, `diagram-family.ts`, `diagram-registry.ts` | Standalone adaptation | Keeps MDX/esbuild evaluation, typed virtual modules, strict capture, discriminated models, JSON-safe compiled values, and generated static browser imports. `check` and `build` share validation before serialization. Review's server bundle is replaced by one CSP-constrained HTML file. |
| `app/src/diagrams.tsx:108-430` | `sequence-model.ts`, `sequence-viewer.tsx` | Mechanical port | Preserves sequence normalization, duplicate anchors, participant ordering, lane geometry, self/straight routes, tour navigation, and source interaction. |
| `src/call-stack-diff.ts:1-169` | `call-stack-diff.ts` | Verbatim | Positional LCS, `>=` removal tie-break, shared-head rows, side-relative depths, tree connectors, patch numbering, and changed-line honesty errors are copied unchanged. Tests cover ordering, identity, and patch lines. |
| `app/src/call-stack-diff.tsx:22-111` | `call-stack-diff-viewer.tsx` | Mechanical host adaptation | Preserves hunk/count/gutter/tint/connector/assertion/location behavior. Desktop session, panel, and telemetry calls become the offline `openEvidence` callback. |
| `app/src/database-lens.tsx:164-190,632-1105` and `src/authoring.ts:236-401,896-1074` | `database-lens-model.ts`, `database-lens-viewer.tsx` | Mechanical model port; offline frame adapter | Keeps ignored non-operation children, exact marker identity, unique use cases, minimum operations, read/write direction, mixed tables/documents, lossless nested typed schemas, nested target resolution, selection, and hydrated source tours. Symbol-backed handles are normalized before JSON. The graph uses Review's SoftwareMapFrame. |
| `src/software-map-model.ts:1-760` | `software-map-model.ts` | Verbatim | The typed C4 input, strict normalization, hierarchy, stable dot paths, data-store metadata, relationships, and validation error aggregation are copied whole. `elementsByPath` is rebuilt only while authoring and omitted from JSON. |
| `app/src/software-map/c4-projection.ts:1-902` | `software-map-c4-projection.ts` | Verbatim except import path | Copies Review's inline-C4 visibility, ancestor collapse, modified/removed filtering, relationship aggregation, collapsed endpoint projection, data-store collection nodes, schema endpoints, foreign keys, and expansion rules. The sole source edit redirects `./model` to the verbatim local `software-map-model.ts`. Oracle vectors run against the copied exports. |
| `app/src/software-map/c4-layout.ts:1-602` | `software-map-c4-layout.ts` | Verbatim | Copies Review's card/group constants, child layout keys, atomic-unit measurement, previous-layout origin preservation, nested placement, and collision displacement. Oracle tests exercise projected node/group boxes. |
| `app/src/software-map/SoftwareMap.tsx:893-918,1374-1810,2187-2375,2395-2690` | `software-map-viewer.tsx`, `software-map-schema.ts`, `software-map-surface.ts` | Exact pure core with offline host adapter | The frame uses Review's initial expansion, change summaries, projection/layout, ancestor collapse, node selection, relationship labels, data-store schema sections, inline/fullscreen portal modes, local Git diff counts, base evidence for removed elements, projected-relationship evidence aggregation, and navigable node/relationship evidence. Remote patch refresh, comments, telemetry, session cache, thread targets, and the desktop CodePeek inspector remain excluded. |
| `app/src/styles.css` | `review-styles.css` | Verbatim | Whole Review stylesheet remains vendored to avoid token/selector drift; `viewer.css` contains only standalone-host additions. |
| `app/src/diagram-tour.tsx`, `CodePeek.tsx` | `viewer.tsx`, `sequence-viewer.tsx`, `viewer.css` | Intentional host adaptation | The standalone host preserves exact embedded source lines, Escape/close and tour navigation. Source-linked clicks dispatch `code-diagram:open-source`; no editor/sidebar is rendered because the embedding host owns that UI. |
| `LICENSE` | `REVIEW_LICENSE`, `THIRD_PARTY_NOTICES.md` | Verbatim notice | Retains the upstream MIT notice and attribution. |

`test/code-diagram.test.ts` protects the four-entry registry, exact call-stack/map vectors, lossless database schemas, hydrated tours, map-backed helpers, path validation, removed-file evidence, typed failures, stale ranges, executable-MDX rejection, one-file output, source-link events without an editor sidebar, and headless-Chrome interaction across all four surfaces.
