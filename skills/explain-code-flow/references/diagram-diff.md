# Flow difference

Inspect changed source and affected callers at both revisions with targeted Git reads. Classify additions, removals, and changed responsibilities, contracts, and routes.

Draw the comparison in fenced Mermaid from verified source facts. A separate unchanged figure is needed only when it explains context the diff cannot show.

Use added (green +), removed (red dashed −), and modified (amber ~), with a legend and text cues. Below each figure, list the actual changes and their revision-qualified source references. Removed code points to the base revision in adjacent text.

Done when every colored element has a matching searchable explanation supported by the revision it describes.
