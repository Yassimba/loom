{
  "name": "teams-slots-prototype",
  "private": true,
  "type": "module",
  "description": "PROTOTYPE — wipe me. Teams Playwright login → Graph getSchedule.",
  "scripts": {
    "start": "node slots.mjs",
    "setup": "node slots.mjs setup --headed",
    "viz": "node slots.mjs viz",
    "browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
